# Use Case: Retrieving users and groups
# Auto-generated from: https://developer.atlan.com/snippets/users-groups/read/
# Generated at: 2026-01-13T02:58:00.309Z

id: users-groups-read
title: "Retrieving users and groups"
category: access-control
icon: zap

description: >-
  You can retrieve users and groups through different helper methods.

externalDocs:
  url: https://developer.atlan.com/snippets/users-groups/read/
  description: Official Atlan documentation

steps:
  - step: 1
    title: "Retrieve all groups"
    description: >-
      0.0.13
      6.1.0
      4.0.0
      
      For example, to retrieve all groups in Atlan:
    notes:
      - type: recommendation
        title: "Paging results"
        content: >-
          Note that you have a limit to control page size, and an offset to control where to start a page.
    method: GET
    endpoint: /api/service/v2/groups
    queryString: 'sort=createdAt&imit=10&offset=0&columns=path&columns=roles'
    examples:
      - key: example-1-1
        summary: "GET /api/service/v2/groups"
  - step: 2
    title: "Retrieve group by name"
    description: >-
      0.0.13
      6.1.0
      4.0.0
      
      To retrieve a specific group in Atlan by its name:
    notes:
      - type: recommendation
        title: "Still returns a list"
        content: >-
          Note that this still returns a list of groups, as it actually runs a contains search for the specified name. You could therefore use this same method to retrieve many groups that all follow the same naming convention, for example.
      - type: recommendation
        title: "Still returns a list"
        content: >-
          Note that this still returns a list of groups, as it actually runs a contains search for the specified name. You could therefore use this same method to retrieve many groups that all follow the same naming convention, for example.
      - type: recommendation
        title: "Still returns a list"
        content: >-
          Note that this still returns a list of groups, as it actually runs a contains search for the specified name. You could therefore use this same method to retrieve many groups that all follow the same naming convention, for example.
      - type: recommendation
        title: "Still returns a list"
        content: >-
          Note that this still returns a list of groups, as it actually runs a contains search for the specified name. You could therefore use this same method to retrieve many groups that all follow the same naming convention, for example.
      - type: recommendation
        title: "URL-encoded filter"
        content: >-
          Note that the filter is URL-encoded. Decoded it would be: {"$and":[{"alias":{"$ilike":"%Example%"}}]}
    method: GET
    endpoint: /api/service/groups
    queryString: 'filter=%7B%22%24and%22%3A[%7B%22alias%22%3A%7B%22%24ilike%22%3A%22%25Example%25%22%7D%7D]%7D'
    examples:
      - key: example-2-1
        summary: "GET /api/service/groups"
  - step: 3
    title: "Retrieve all users"
    description: >-
      0.0.13
      6.1.0
      4.0.0
      
      To retrieve all users in Atlan:
    notes:
      - type: recommendation
        title: "Paging results"
        content: >-
          Note that you have a limit to control page size, and an offset to control where to start a page.
    method: GET
    endpoint: /api/service/users
    queryString: 'sort=username&limit=100&offset=0'
    examples:
      - key: example-3-1
        summary: "GET /api/service/users"
  - step: 4
    title: "Retrieve user by username"
    description: >-
      0.0.13
      1.3.3
      4.0.0
      
      To retrieve a specific user in Atlan by their username:
    notes:
      - type: recommendation
        title: "URL-encoded filter"
        content: >-
          Note that the filter is URL-encoded. Decoded it would be: {"username":"jdoe"}
    method: GET
    endpoint: /api/service/users
    queryString: 'filter=%7B%22username%22%3A%22jdoe%22%7D'
    examples:
      - key: example-4-1
        summary: "GET /api/service/users"
  - step: 5
    title: "Retrieve user by email"
    description: >-
      0.0.13
      6.1.0
      4.0.0
      
      To retrieve a specific user in Atlan by their email address:
    notes:
      - type: recommendation
        title: "Still returns a list"
        content: >-
          Note that this still returns a list of users, as it actually runs a contains search for the specified email address. You could therefore use this same method to retrieve many users that all have the same email domain, for example.
      - type: recommendation
        title: "Still returns a list"
        content: >-
          Note that this still returns a list of users, as it actually runs a contains search for the specified email address. You could therefore use this same method to retrieve many users that all have the same email domain, for example.
      - type: recommendation
        title: "Still returns a list"
        content: >-
          Note that this still returns a list of users, as it actually runs a contains search for the specified email address. You could therefore use this same method to retrieve many users that all have the same email domain, for example.
      - type: recommendation
        title: "Still returns a list"
        content: >-
          Note that this still returns a list of users, as it actually runs a contains search for the specified email address. You could therefore use this same method to retrieve many users that all have the same email domain, for example.
      - type: recommendation
        title: "URL-encoded filter"
        content: >-
          Note that the filter is URL-encoded. Decoded it would be: {"email":{"$ilike":"%@example.com%"}}
    method: GET
    endpoint: /api/service/users
    queryString: 'filter=%7B%22email%22%3A%7B%22%24ilike%22%3A%22%25%40example.com%25%22%7D%7D'
    examples:
      - key: example-5-1
        summary: "GET /api/service/users"
  - step: 6
    title: "Retrieve multiple users"
    description: >-
      0.0.13
      6.1.0
      4.0.0
      
      To retrieve multiple users in Atlan by their usernames:
    notes:
      - type: recommendation
        title: "URL-encoded filter"
        content: >-
          Note that the filter is URL-encoded. Decoded it
          would be: {"username":{"$in":["john.doe","jane.doe"]}}
      - type: recommendation
        title: "URL-encoded filter"
        content: >-
          Note that the filter is URL-encoded. Decoded it
          would be: {"email":{"$in":["john@atlan.com","jane@atlan.com"]}}
    method: GET
    endpoint: /api/service/users
    queryString: 'filter={%22username%22:{%22$in%22:[%22john.doe%22,%22jane.doe%22]}}'
    examples:
      - key: example-6-1
        summary: "GET /api/service/users"
      - key: example-6-2
        summary: "GET /api/service/users"
  - step: 7
    title: "By usernames"
    description: >-
      To retrieve multiple users in Atlan by their usernames:
    method: GET
    endpoint: /api/service/users
    queryString: 'filter={%22username%22:{%22$in%22:[%22john.doe%22,%22jane.doe%22]}}'
    examples:
      - key: example-7-1
        summary: "GET /api/service/users"
  - step: 8
    title: "By emails"
    description: >-
      To retrieve multiple users in Atlan by their emails:
    method: GET
    endpoint: /api/service/users
    queryString: 'filter={%22email%22:{%22$in%22:[%22john@atlan.com%22,%20%22jane@atlan.com%22]}}'
    examples:
      - key: example-8-1
        summary: "GET /api/service/users"
  - step: 9
    title: "Retrieve user group membership"
    description: >-
      0.0.13
      2.0.1
      4.0.0
      
      To retrieve the groups a user is a member of:
    notes:
      - type: recommendation
        title: "User ID in the URL"
        content: >-
          Note that you must provide the unique ID (GUID) of the user to retrieve its associated groups.
      - type: recommendation
        title: "Group ID in the URL"
        content: >-
          Note that you must provide the unique ID (GUID) of the group to retrieve its associated members.
    method: GET
    endpoint: /api/service/users/f06122f4-7279-4e42-b9e0-46f9b470e659/groups
    examples:
      - key: example-9-1
        summary: "GET /api/service/users/f06122f4-7279-4e42-b9e0-46f9b470e659/groups"
      - key: example-9-2
        summary: "GET /api/service/groups/e79cb8eb-2bb6-4821-914c-f8dfd21fedc7/members"
  - step: 10
    title: "Retrieve groups for a user"
    description: >-
      0.0.13
      2.0.1
      4.0.0
      
      To retrieve the groups a user is a member of:
    method: GET
    endpoint: /api/service/users/f06122f4-7279-4e42-b9e0-46f9b470e659/groups
    examples:
      - key: example-10-1
        summary: "GET /api/service/users/f06122f4-7279-4e42-b9e0-46f9b470e659/groups"
  - step: 11
    title: "Retrieve users in a group"
    description: >-
      0.0.13
      2.0.1
      4.0.0
      
      To retrieve the users that are members of a group:
    method: GET
    endpoint: /api/service/groups/e79cb8eb-2bb6-4821-914c-f8dfd21fedc7/members
    examples:
      - key: example-11-1
        summary: "GET /api/service/groups/e79cb8eb-2bb6-4821-914c-f8dfd21fedc7/members"
