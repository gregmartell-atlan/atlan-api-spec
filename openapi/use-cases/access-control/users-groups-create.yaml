# Use Case: Creating users and groups
# Auto-generated from: https://developer.atlan.com/snippets/users-groups/create/
# Generated at: 2026-01-13T02:58:00.309Z

id: users-groups-create
title: "Creating users and groups"
category: access-control
icon: zap

description: >-
  Like other objects in the SDK, you can create users and groups using the builder pattern.

externalDocs:
  url: https://developer.atlan.com/snippets/users-groups/create/
  description: Official Atlan documentation

steps:
  - step: 1
    title: "Create a group"
    description: >-
      0.0.13
      1.3.3
      4.0.0
      
      For example, to create a group:
    notes:
      - type: warning
        title: "Must follow certain constraints"
        content: >-
          The internal name for the group must be unique, all lowercase, and include only alphanumeric characters and the _ (no spaces or special characters).
    method: POST
    endpoint: /api/service/groups
    examples:
      - key: example-1-1
        summary: "POST /api/service/groups"
        request: {
                    "group": {
                              "attributes": {
                                        "alias": [
                                                  "Example Group"
                                        ],
                                        "isDefault": [
                                                  "false"
                                        ]
                              },
                              "name": "example_group"
                    }
          }
  - step: 2
    title: "Create a user"
    description: >-
      To create a user, what you're really doing is inviting them. The users will need to verify their email address to activate their account, and will be able to specify their own password as part of that process.
      
      0.0.13
      2.0.4
      4.0.0
      
      To invite a user:
    notes:
      - type: warning
        title: "You probably need to look this up first"
        content: >-
          When using the raw API, you will need to lookup the role GUID yourself. You can GET /api/service/roles, and the GUID will be the id field in the response for each role.
    method: POST
    endpoint: /api/service/users
    examples:
      - key: example-2-1
        summary: "POST /api/service/users"
        request: {
                    "users": [
                              {
                                        "email": "someone@somewhere.org",
                                        "roleName": "$member",
                                        "roleId": "be5f0df7-dab8-4107-8bf0-56ce7131623a"
                              }
                    ]
          }
